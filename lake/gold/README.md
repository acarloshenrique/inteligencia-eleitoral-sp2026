Gold layer.

- Publishes decision-ready tables, scores, marts, and dashboard/report datasets.
- `api/`, `workers/`, and `presentation/` must consume only gold outputs.
- Catalog and serving artifacts derived from curated datasets belong here.
