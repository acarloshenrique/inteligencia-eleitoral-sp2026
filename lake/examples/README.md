# Exemplos

Os exemplos Parquet sao gerados em testes para evitar versionar dados eleitorais ou binarios no Git. Use `BaseLakehouseTransformation.write_dataframe` para materializar amostras em ambiente local.
