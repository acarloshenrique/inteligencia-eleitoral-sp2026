Silver layer.

- Normalizes schema, types, dates, keys, and encoding.
- Produces canonical datasets ready for joins and validation.
- Serves as the contract boundary between raw capture and analytical marts.
